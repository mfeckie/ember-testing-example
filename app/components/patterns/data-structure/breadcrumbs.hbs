{{#if (has-block)}}
  {{yield}}
{{else}}
  {{#each this.withDividers as | bci |}}
    {{#if bci.divider}}
      <Patterns::DataStructure::Breadcrumbs::Divider />
    {{else}}
      <Patterns::DataStructure::Breadcrumbs::Link @href={{bci.href}}>
        {{bci.text}}
      </Patterns::DataStructure::Breadcrumbs::Link>
    {{/if}}
  {{/each}}
{{/if}}